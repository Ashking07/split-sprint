/**
 * Catch-all API handler for /api/* routes. Matches /api/health, /api/auth/login, etc.
 * Preserves the full request path for Express routing.
 */
import app from "../server.js";

export default app;
